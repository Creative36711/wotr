#!/usr/bin/env python3
"""
ВСЕ ЗАДЕРЖКИ СЦЕНАРИЯ В ОДНОМ МЕСТЕ.

Измени значение — и вся автоматизация подстроится. Все величины в СЕКУНДАХ.
Правь только числа, не трогай имена ключей (их использует main.py / menu.py).

Если клики срабатывают РАНЬШЕ, чем экран успел отрисоваться — увеличь
соответствующую задержку. Если ждёшь слишком долго — уменьши.
"""

TIMINGS = {
    # Сколько ждать появления окна игры (game.dat) после запуска.
    "window_timeout": 120.0,

    # РЕЗЕРВ: фиксированное ожидание главного меню. Используется ТОЛЬКО если
    # визуального маркера нет (config/menu_marker.* не снят через calibrate F8).
    # Основной механизм — визуальный детект по маркеру (bridge/visual_ready.py),
    # который сам ждёт появления меню и не зависит от холодного/тёплого запуска.
    "menu_load": 16.0,

    # Период сэмплирования для визуального детекта маркера, сек.
    "menu_poll": 1.0,

    # Максимум ожидания появления маркера (холодный запуск может быть долгим).
    "menu_timeout": 60.0,

    # Пауза ПОСЛЕ обнаружения готового меню и ВЫВЕДЕНИЯ окна на передний план,
    # ПЕРЕД первым move/click. При холодном запуске игра появляется, но ещё
    # ~0.5-1 c не принимает инжектируемый ввод — без этой паузы первые клики
    # уходят «в пустоту».
    "menu_settle": 1.2,

    # Сколько держать курсор над «Сеть», пока не раскроется выпадающее меню.
    "hover_network": 0.8,

    # Сколько ждать экран лобби после клика «Лок. сеть».
    # (с запасом: после холодного старта экраны грузятся медленнее)
    "lobby_load": 2.0,

    # ---- верификация экрана «Лок. сеть» по маркеру-картинке --------------

    # Сколько ждать появления маркера «Лок. сеть» (_tools/Маркер локальной сети.jpg)
    # после клика. Если маркер не найден за это время — клики ушли «в пустоту»
    # (холодный старт: игра ещё не принимала ввод), навигацию повторяем.
    "lan_marker_timeout": 2.5,

    # Период опроса кадра при проверке маркера, сек.
    "lan_marker_poll": 0.5,

    # ---- комната создания игры ----

    # Сколько ждать появления комнаты после клика «Создать игру».
    "room_load": 2.0,

    # Сколько ждать раскрытия списка сложности после клика по слоту.
    "room_dropdown": 0.6,

    # Пауза после выбора сложности, перед кликом по следующему слоту.
    "room_settle": 0.4,

    # Пауза между кликами при назначении стартовой позиции на карте.
    "start_pos_click": 0.2,

    # Сколько держать блокировку после «Начать игру» (5-сек отсчёт + запас,
    # чтобы пользователь не успел вмешаться до загрузочного экрана).
    "start_countdown": 7.0,
}
