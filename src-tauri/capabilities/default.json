{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main campaign window",
  "windows": [
    "main",
    "battle-mask"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "core:window:allow-set-title"
  ]
}
